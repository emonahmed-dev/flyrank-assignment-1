HTML, CSS, TailwindCss, JavaScript, React.js, Next.js

All forms must use react-hook-form paired with zod schema validation.

Never use uncontrolled inputs or inline state without explicit TypeScript types.

Every interactive input must include accessible label tags and aria-invalid attributes.